spring:
  application:
    name: msvc-products

  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://config-server:8888}
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        multiplier: 1.5